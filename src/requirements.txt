litellm==1.74.9
asyncpg
pg8000  # Pure Python PostgreSQL driver for synchronous operations in subprocesses
aiosqlite
aiofiles>=23.0.0  # For async file I/O operations
pydantic-settings==2.10.1
cryptography
python-multipart
databricks
databricks-sdk==0.102.0  # Lakebase Autoscaling requires >=0.101.0 (X-Databricks-Org-Id header)
databricks-vectorsearch==0.63  # Pinned from uv.lock to prevent pip backtracking
croniter
crewai[tools]==1.14.5  # Unified cognitive memory system (1.10.1+); pinned - [tools] extra installs crewai-tools package
pydantic[email]==2.11.10  # Pinned from uv.lock to prevent pip backtracking
passlib
email-validator
google-api-python-client
pysendpulse
langchain==1.2.15  # Pinned from uv.lock to prevent pip backtracking
nixtla
greenlet
wheel
selenium
python-pptx
urllib3>=1.26.6
mcp>=1.26.0,<1.27.0  # Required by crewai 1.14.5
mcpadapt==0.1.10
pytest
pytest-asyncio

# Dependency conflict resolution - pinned from uv.lock
bcrypt==4.0.1
starlette==0.40.0
httpx-sse==0.4.0  # Required by cohere 5.18.0
rich>=14.2.0  # Required by textual>=7.5.0 which is a transitive dep of crewai 1.14.5
openai>=2.0.0,<3  # Required by crewai 1.14.5
instructor==1.15.1  # Pinned from uv.lock; >=1.13 needed for openai 2.x (crewai 1.14.5 requirement)
langchain-core==1.3.1  # Pinned from uv.lock to prevent pip backtracking
langchain-openai==1.2.0  # Pinned from uv.lock; 1.x needed for openai 2.x compatibility
langsmith==0.6.9  # Pinned from uv.lock to prevent pip backtracking
chromadb==1.1.1  # Pinned from uv.lock to prevent pip backtracking
sse-starlette==3.0.3  # Pinned from uv.lock to prevent pip backtracking
grpcio==1.76.0  # Pinned from uv.lock to prevent pip backtracking

# Add any other dependencies your project requires below
pytest-cov
psutil  # For process management and cleanup in ProcessCrewExecutor
mlflow==3.4.0  # For LLM observability and tracking in Databricks
azure-identity>=1.15.0  # For Power BI authentication (UsernamePasswordCredential)
protobuf>=5.29.0,<6.0.0  # mem0ai compatibility
databricks-agents==1.4.0  # Pinned from uv.lock to prevent pip backtracking
grpcio-status==1.70.0  # Pinned from uv.lock - compatible with protobuf <6.0.0

# OpenTelemetry - Structured tracing with span hierarchy
opentelemetry-sdk>=1.27.0,<2.0.0
opentelemetry-api>=1.27.0,<2.0.0
opentelemetry-exporter-otlp-proto-http>=1.27.0,<2.0.0
opentelemetry-exporter-otlp-proto-grpc>=1.27.0,<2.0.0
openinference-instrumentation-crewai>=1.1.3,<2.0.0  # 1.x was a breaking release for crewai 1.10+ Pydantic Agent/Crew

# Security - Prompt injection detection
